describe('stencil', () => {
  it('should', () => {
    expect(
      stencil({
        parts: [
          {
            name: 'container',
            props: {},
          },
        ],
      })
    ).toBeTruthy()
  })
})
